# Loaded ONLY by `npm run dev`. `npm run build` runs in production mode and
# ignores this file, so .env stays the production config and there is nothing
# to remember to toggle before a deploy.
VITE_API_URL="http://localhost:4000"
